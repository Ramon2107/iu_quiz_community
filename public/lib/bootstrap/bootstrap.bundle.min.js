/*!
 * Bootstrap v5.3.0 (https://getbootstrap.com/)
 * Copyright 2011-2023 The Bootstrap Authors (https://github.com/twbs/bootstrap/graphs/contributors)
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)
 */
!function(t,e){"object"==typeof exports&&"undefined"!=typeof module?module.exports=e():"function"==typeof define&&define.amd?define(e):(t="undefined"!=typeof globalThis?globalThis:t||self).bootstrap=e()}(this,(function(){"use strict";

/* This is a minimal version of Bootstrap JS for demonstration purposes */
/* In a real application, you would include the full Bootstrap JS file */

// Define a minimal bootstrap object with essential functionality
const bootstrap = {
  version: '5.3.0',
  
  // Modal functionality
  Modal: class {
    constructor(element, options) {
      this._element = element;
      this._options = options || {};
      this._isShown = false;
    }
    
    static get NAME() {
      return 'modal';
    }
    
    toggle() {
      return this._isShown ? this.hide() : this.show();
    }
    
    show() {
      if (this._isShown) return;
      this._isShown = true;
      this._element.style.display = 'block';
      this._element.classList.add('show');
      document.body.classList.add('modal-open');
    }
    
    hide() {
      if (!this._isShown) return;
      this._isShown = false;
      this._element.classList.remove('show');
      this._element.style.display = 'none';
      document.body.classList.remove('modal-open');
    }
    
    static getInstance(element) {
      return element && element._modal;
    }
  },
  
  // Dropdown functionality
  Dropdown: class {
    constructor(element, options) {
      this._element = element;
      this._options = options || {};
      this._menu = element.querySelector('.dropdown-menu');
      this._isShown = false;
    }
    
    static get NAME() {
      return 'dropdown';
    }
    
    toggle() {
      return this._isShown ? this.hide() : this.show();
    }
    
    show() {
      if (this._isShown) return;
      this._isShown = true;
      this._menu.classList.add('show');
    }
    
    hide() {
      if (!this._isShown) return;
      this._isShown = false;
      this._menu.classList.remove('show');
    }
  },
  
  // Alert functionality
  Alert: class {
    constructor(element) {
      this._element = element;
    }
    
    static get NAME() {
      return 'alert';
    }
    
    close() {
      this._element.classList.remove('show');
      setTimeout(() => {
        this._element.remove();
      }, 150);
    }
  },
  
  // Tab functionality
  Tab: class {
    constructor(element) {
      this._element = element;
    }
    
    static get NAME() {
      return 'tab';
    }
    
    show() {
      const target = document.querySelector(this._element.getAttribute('data-bs-target'));
      if (!target) return;
      
      // Hide all tabs
      const parent = target.parentNode;
      const siblings = parent.querySelectorAll('.tab-pane.active');
      siblings.forEach(sibling => {
        sibling.classList.remove('active', 'show');
      });
      
      // Show current tab
      target.classList.add('active', 'show');
      
      // Update nav links
      const navLinks = document.querySelectorAll('[data-bs-toggle="tab"]');
      navLinks.forEach(link => {
        link.classList.remove('active');
      });
      this._element.classList.add('active');
    }
  },
  
  // Collapse functionality
  Collapse: class {
    constructor(element, options) {
      this._element = element;
      this._options = options || {};
      this._isShown = false;
      this._target = document.querySelector(element.getAttribute('data-bs-target'));
    }
    
    static get NAME() {
      return 'collapse';
    }
    
    toggle() {
      return this._isShown ? this.hide() : this.show();
    }
    
    show() {
      if (this._isShown || !this._target) return;
      this._isShown = true;
      this._target.classList.add('show');
      this._element.setAttribute('aria-expanded', true);
    }
    
    hide() {
      if (!this._isShown || !this._target) return;
      this._isShown = false;
      this._target.classList.remove('show');
      this._element.setAttribute('aria-expanded', false);
    }
  }
};

// Return the bootstrap object
return bootstrap;

}));